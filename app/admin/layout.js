export const metadata = {
  title: 'Elira Atelier — Admin',
  description: 'Administrator dashboard',
}

export default function AdminLayout({ children }) {
  return <div className="min-h-screen bg-[#faf7f2]">{children}</div>
}
